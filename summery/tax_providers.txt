Feature Name: Tax Providers

Endpoint: GET /admin/tax-providers
  OperationId: GetTaxProviders
  Summary: List Tax Providers
  Security: api_token, cookie_auth, jwt_token
  Query Parameters:
    - fields, in=query, type=string, optional
    - offset, in=query, type=number, optional
    - limit, in=query, type=number, optional
    - order, in=query, type=string, optional
    - id, in=query, type=oneOf, optional
    - is_enabled, in=query, type=boolean, optional
    - $and, in=query, type=array, optional
    - $or, in=query, type=array, optional
    - with_deleted, in=query, type=boolean, optional
  Responses:
    - Status: 200 - OK
      Content-Type: application/json
    Model: gettaxproviders_body
    Type: object
    Properties:
  - limit: number, (required)
  - offset: number, (required)
  - count: number, (required)
  - estimate_count: number, (optional)
  - tax_providers: array, (required), items=object (AdminTaxProvider)
    - Status: 400
      (no content)
    - Status: 401
      (no content)
    - Status: 404
      (no content)
    - Status: 409
      (no content)
    - Status: 422
      (no content)
    - Status: 500
      (no content)
  Referenced Models:
    Model: AdminTaxProvider
    Type: object
    Description: The tax provider's details.
    Properties:
  - id: string, (required)
  - is_enabled: boolean, (required)

